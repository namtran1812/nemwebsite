# Nem Website — Terminal Coming Soon Page

Copy `index.html` and `vercel.json` into the root of the repository, then commit and push to the branch connected to Vercel.

```bash
git add index.html vercel.json
git commit -m "Add terminal-style coming soon page"
git push origin main
```

The Vercel rewrite sends every route to the temporary landing page.
